# sql-debug-env
Code LLMs are trained to imitate syntax, making them prone to semantic errors in data tasks. Solution: An execution-based RL environment using an in-memory DuckDB sandbox to dynamically execute, verify, and grade AI-generated SQL and ETL scripts with dense rewards.
